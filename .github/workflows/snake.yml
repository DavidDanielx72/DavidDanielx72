name: Generate Snake Animation

permissions:
  contents: write

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: Platane/snk@v3
        id: snake-gif
        with:
          github_user_name: DavidDanielx72
          # Eye-catching color scheme - Neon Rainbow
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=#FFD700&color_dots=#0e4429,#006d32,#26a641,#39d353,#FFD700
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark&color_snake=#FFD700
            dist/github-contribution-grid-snake-rainbow.svg?color_snake=#FF6B6B,#FF8E8E,#FFD700,#4ECDC4,#6EE7DE
          
      - uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
